/**
 * Servicio de API para productos.
 * Usa asyncMock (datos en memoria, simula peticiones asíncronas).
 * Misma interfaz que tendría con fetch a un backend: getAll, getById, create, update, delete.
 */
export { productsApi } from "../asyncMock.js";
